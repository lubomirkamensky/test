# TEST-0002: Establish story patterns in test epic: pattern scaffolding and validation harness

> Establish story patterns in test epic: pattern scaffolding and validation harness

**Type:** default
**Epic:** [TEST](TEST)
**Status:** completed
**Blueprint:** [implementation-story-blueprint](implementation-story-blueprint)

## Goal

The test epic has an empty `pattern/` folder. Seed it with the first story pattern
blueprints and prove that story lifecycle commands and a second, parallel session can
consume them. This story is one of two deliberately parallel tasks (sibling: TEST-0003
in subepic test-sandbox, executed in a separate Claude session).

## Requirements

### R1: Epic-level story pattern blueprints

**Acceptance Criterion:** `test/_grid4d/pattern/` contains at least two pattern
documents following ahabase naming (`<scope>-<unique-name>.md`, scope = `test`),
e.g. `test-story-simulation-pattern.md` (how simulation stories are framed: purpose,
participants, defect capture protocol) and `test-parallel-session-protocol.md` (how
two parallel stories coordinate: disjoint file territory, shared-state watchpoints,
landing order).

### R2: Patterns are consumable, not decorative

**Acceptance Criterion:** Each pattern states at least one concrete, checkable rule
that a story execution can follow or violate; TEST-0002's own execution visibly
follows the patterns it introduces (self-application).

### R3: Parallel-session readiness evidence

**Acceptance Criterion:** The story document or notes record which shared state the
two sessions touch (active_story in `_data/evolucean/state.json`, git refs, counters
branch) and what was observed when both sessions ran, including any defect captured
into an issue or an Evolucean story.

## Acceptance Criteria

- [x] Two or more pattern documents exist in `test/_grid4d/pattern/` with ahabase-compliant names
- [x] Each pattern carries at least one checkable rule
- [x] Parallel-session observations recorded (shared state, defects)

## Notes

Created: 2026-09-13
Sibling story: the subepic sibling was minted as **TEST-SANDBOX-0001** (subepics
carry their own prefix and counter; `--subepic test-sandbox`). File territory is
disjoint: TEST-0002 owns `test/_grid4d/pattern/` (epic level), TEST-SANDBOX-0001
owns `test/_grid4d/subepic/test-sandbox/`.

### Shared state watchpoints

Observations so far (2026-09-13, single session driving both stories until the
second session opens):

- **Epic resolution default** — `story create` without `--epic` minted into the
  primary epic (EVOLUCEAN-0281) even though cwd was the test epic; the ghost
  story was pruned. Guard proposed: warn when calling tree and resolved epic
  disagree.
- **Counters branch** — sequential minting (TEST-0002, then TEST-SANDBOX-0001)
  reserved both IDs atomically; no race observed yet (single writer).
- **active_story** — one global slot; switching stories required explicit
  `pause`/`resume`; the second session must `resume TEST-SANDBOX-0001` and the
  swap will be the first true two-writer observation.
- **Worktree layout** — flat `_worktrees/test-0003` violated the hierarchical
  convention mid-run and was `git worktree move`-d into
  `_worktrees/test/test-sandbox/TEST-SANDBOX-0001`.
- **Story file minting** — `story create` in the test epic minted the story
  `.md` but not a `-tests.md` template (both files were minted in the primary
  epic); tests file created manually here.
- **km_logger placement** — the knowledge map was created in the main tree
  (where the story was first read) and had to be moved into the story worktree;
  watch whether the hook keeps writing into the worktree now.
- **Stale bound tree after worktree move (defect)** — the hook kept writing the
  knowledge map into the main tree even after all work moved to the worktree.
  Root cause: the collaboration scope's `tree` was bound at story-create time
  (main tree) and the resume fast path (`Already active`, hooks.py) returns
  without rebinding, so `log_file_access` faithfully targeted the stale tree.
  Workaround applied: `pause` + `resume` from the worktree (fresh bind updates
  scope["tree"] via resolve_epic_root_from_cwd). Candidate occurrence for the
  Evolucean issue `km-logger-writes-wrong-tree`; related to
  `story-create-worktree-bootstrap-manual` (a worktree-aware create would bind
  the right tree from the start).

### Pre-run baseline for R3 (captured 2026-09-13, before the second session)

- `state.json`: active_story `TEST-0002`, active_epic `test`
- session scopes: this session bound to `TEST-0002 @ _worktrees/test/TEST-0002`;
  two stale scopes from older sessions (`None @ _worktrees/evolucean/EVOLUCEAN-0277`,
  `None @ None`)
- `git worktree list` (test epic): main tree on `main`, TEST-0002 worktree on
  `d4d26b2`, TEST-SANDBOX-0001 worktree on `main` tip (`6e2926e`)
- `origin/main` (test) at `6e2926e`; counters branch exists at `1d2a88ee`

Watch during the parallel run: active-story clobber on resume, scope tree of the
second session, counters branch races, hook log targets, and the two landings
into `main`.

### Parallel-run observations (R3)

**O1 — active-story slot is single and last-writer-wins (2026-09-13 ~15:10):**
the second session's `resume TEST-SANDBOX-0001` overwrote `state.json`
active_story from `TEST-0002` to `TEST-SANDBOX-0001` with no warning to either
session. Symmetric risk: when this session re-resumes TEST-0002 to complete, it
will clobber the second session's pointer the same way. Per-session scope
(`sessions/<id>.json` `bound_story`) is unaffected — hook attribution keeps
working in both sessions; only the global pointer moves.

**O2 — worktree-born session binds correctly:** the second session's scope
shows `tree = _worktrees/test/test-sandbox/TEST-SANDBOX-0001` — a story created
*inside* its worktree binds the right tree from the start (contrast with the
stale-bound-tree defect above, which only bites when a story moves to a
worktree after creation).

**O3 — capture recursion:** minting the Evolucean capture story (EVOLUCEAN-0283,
by the sandbox session from the evolucean main tree) flipped the global active
slot yet again — the capture mechanism reproduces the defect it captures.

**O4 — pause unbinds the calling scope:** pausing EVOLUCEAN-0282 (a story this
session had just created from the evolucean main tree) unbound *this* session's
scope from TEST-0002 (bound_story went None, tree went to the pause-time cwd);
required a resume from the TEST-0002 worktree to restore attribution.

**O5 — parallel capture collides on one issue (protocol R4 violated by the
capture stories themselves):** EVOLUCEAN-0282 (worktree) and EVOLUCEAN-0283
(main tree) both wrote occurrences into `km-logger-writes-wrong-tree` at the
same time from their own trees — overlapping hunks in the occurrences table,
duplicitous evidence for one fact (stale bound tree), divergent status edits
(0282 reopens the issue). The file-territory rule of the parallel-session
protocol was declared for the test stories but not honored by the capture
stories; expect a non-ff CAS rejection and a manual occurrence merge at
landing time.

**O6 — stop handler is cwd-blind for the active story:** with this session's
shell cwd left at the evolucean main tree (after issue audits), the stop
handler's GAME STATUS reported "Story: TEST-0002 / LLM: Create TEST-0002.md" —
the file exists in the story worktree, but the handler resolved the story path
from the calling cwd instead of the scope binding. Same resolution family as
the stale-bound-tree defect (km-logger-writes-wrong-tree), observer side:
scopes hold the truth, some hook paths still trust the cwd.

**O7 — capture recursion again (by design):** documenting O6 required an
Evolucean story (change guard); minting EVOLUCEAN-0284 flipped the global
active slot once more, mid-run. The capture mechanism keeps reproducing the
defect it captures — recorded in the 0284 issue as occurrence 3 of
`active-story-global-across-parallel-sessions`.

### Issue ledger update (same day, later)

- New issue from the sandbox session's observations (untracked in the main
  tree, pending its landing): `status-banner-shows-namespace-not-active-epic`
  (observation 5 of TEST-SANDBOX-0001 — cosmetic).
- New issue written by this session under EVOLUCEAN-0284:
  `stop-handler-story-resolution-cwd-blind` (open; O6 above; same family as
  `km-logger-writes-wrong-tree`, candidate fix = resolve via scope `tree`).

### Issue ledger (2026-09-13, parallel run)

New issues (written by the sandbox session under EVOLUCEAN-0283):

- `active-story-global-across-parallel-sessions` (open, defect) — single global
  active slot silently stolen on every create/resume across live sessions;
  steal chain TEST-0002 → TEST-SANDBOX-0001 → EVOLUCEAN-0282 → EVOLUCEAN-0283
  recorded with two occurrences and a session-aware-slot solution direction.
- `km-blueprint-check-false-alarm-on-fresh-knowledge-map` (open, minor) — the
  hook flags the fresh hook-created map "Missing required sections: Created"
  on first read; self-resolving, teaches operators to ignore validation.

Updated issues (by the same session):

- `story-create-invalid-branch-name-from-description` — new occurrence: 0283
  branch slug mangles intra-word hyphens ("parallel-session" →
  "parallelsession").

Pending occurrences from THIS story's run (recorded in Watchpoints above, to be
written into the matching issues by the Evolucean capture story — the change
guard requires an Evolucean story for issue edits):

- `km-logger-writes-wrong-tree` ← stale bound tree / "Already active" no-rebind.
- `story-prefix-mismatch` ← silent epic default minted ghost EVOLUCEAN-0281.
- `story-create-worktree-bootstrap-manual` ← `-tests.md` template not minted in
  non-primary epics.

### Landing results (R3 finále, 2026-09-13)

- **TEST-SANDBOX-0001** — landed: completion commit `71323c3` CAS-pushed to
  `origin/main` (test) from the story worktree; the local test main tree was
  left behind ("behind by 2 commits") and fast-forwarded manually. Recorded as
  issue `worktree-complete-leaves-local-main-behind` (S6, EVOLUCEAN-0285).
- **EVOLUCEAN-0282** — landed as v0.0282 with the defect triage (D1-D4,
  B1-B4, M1-M2) and candidate stories.
- **EVOLUCEAN-0283** — completion ran into the O5-predicted merge conflict on
  `km-logger-writes-wrong-tree` exactly at landing time (the earlier
  de-escalation moved the collision, it did not remove it). Resolved by a
  manual occurrence merge: the 0282 full row kept, the TEST-0002 observer row
  merged in, the duplicate third row (same fact from the 0283 viewpoint)
  dropped. Landing finished manually: CAS push, ff local main, tag
  v0.0283.2.0008.00.
- **EVOLUCEAN-0284** — landed as v0.0284 (stop-handler cwd-blind issue).
- **EVOLUCEAN-0285** — consolidation story (session-isolation triage S1-S6)
  landed as v0.0285 with two post-landing occurrences: the cross-epic KM leak
  (`EVOLUCEAN-0282-knowledge-map.md` minted inside the TEST epic's story
  folder — slot×cwd composition across epic boundaries) and the global slot
  left pointing at a completed story.
- **New isolation defect found during this finále** — `story_pool` is an LRU
  of the last five accesses and `resume <STORY>` searches only that list: a
  live in-progress story (TEST-0002) became unresumable ("not found in
  pool") once five newer accesses evicted it. Workaround: refreshed the LRU
  entry in `state.json` by hand. Belongs to the S1 family (session-aware
  slots); issue write-up pending an Evolucean story (change guard).

### Delivery vs. goal (honest accounting)

Delivered per the letter of the ACs: R1 (two patterns with checkable rules),
R2 (self-application), R3 (O1-O7, ledger, landing evidence above). What the
run did NOT demonstrate: the patterns were never consumed by an independent
third story — the "validation harness" promised in the Goal reduced to
self-application plus defect capture. The sibling story's landed document
still has unchecked AC boxes (its completion flow did not enforce them), and
its pattern-consumption evidence lives in its knowledge map without a
pattern-driven verdict. Defect capture legitimately dominated (the R3 AC
asks for captured defects), but the consumption wave is the missing piece:
follow-up candidate for the test epic — a fresh story that reads the
patterns first and executes purely from them, checked ACs included.

